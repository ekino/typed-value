spring.application.name=typed-value-integration-tests

# JPA
spring.jpa.hibernate.ddl-auto=create-drop

# Elasticsearch - Auto-create indices from @Document classes
spring.data.elasticsearch.repositories.enabled=true
